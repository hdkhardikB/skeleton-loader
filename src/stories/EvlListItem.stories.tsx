import * as React from 'react';
import { storiesOf } from '@storybook/react';
import { EvlListItem } from '@components';

const stories = storiesOf('EvlListItem', module);

stories.add('dummy', () => <EvlListItem />);
