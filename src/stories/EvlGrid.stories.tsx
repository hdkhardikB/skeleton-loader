import * as React from 'react';
import { storiesOf } from '@storybook/react';
import { EvlGrid } from '@components';

const stories = storiesOf('EvlGrid', module);

stories.add('dummy', () => <EvlGrid />);
