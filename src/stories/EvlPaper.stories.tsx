import * as React from 'react';
import { storiesOf } from '@storybook/react';
import { EvlPaper } from '@components';

const stories = storiesOf('EvlPaper', module);

stories.add('Paper component', () => <EvlPaper />);
